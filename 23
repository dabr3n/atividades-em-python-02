// 23)Faça um programa que peça o tamanho de um arquivo para download (em MB) e a velocidade
de um link de Internet (em Mbps), calcule e informe o tempo aproximado de download do arquivo
usando este link (em minutos).

t = float(input("Insira o tamanho do arquivo em MB: "))
v = float(input("Insira a velocidade da internet em MBPS: "))

tem = t/v
m = tem*60

print("O tempo aproximado é de: ", m)
